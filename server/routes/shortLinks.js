/**
 * SHORT LINKS FEATURE - DEPRECATED
 * This file is no longer used. PDFs are accessed directly via pdfUrl from MongoDB.
 * All routes have been disabled and removed.
 */

const express = require("express");
const router = express.Router();

// This route is no longer used - short links feature has been removed
// PDFs are now accessed directly via the pdfUrl field from the database

module.exports = router;

